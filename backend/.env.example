# Environment example for news_app
# Copy this to .env and fill values (do NOT commit secrets).

# Resend (preferred)
RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXX
EMAIL_FROM="News App <no-reply@yourdomain.com>"

# Cron schedule (default daily at 08:00). For testing use '*/1 * * * *' (every minute)
DAILY_EMAIL_CRON=0 8 * * *
TIMEZONE=America/New_York

# MongoDB (optional override; default in code is mongodb://localhost:27017/news_app)
MONGO_URI=mongodb://localhost:27017/news_app

# Optional SMTP fallback (if you prefer SMTP instead of Resend)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_pass
SMTP_SECURE=false

# Example for SendGrid (if using SMTP relay)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=SG.xxxxxxxxxxxxxxxxxxxxxxxxx

# Notes:
# - After editing .env, restart your Node process so the new values are loaded.
# - Keep credentials out of source control. Use environment variables or a secrets manager in production.
